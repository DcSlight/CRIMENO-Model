# Core
pyzmq
opencv-python
numpy
Pillow
websockets

# YOLO tracker
ultralytics

# VLM (uses torch only for the tracker; inference is via Gemini below)
torch

# VLM vision (Gemini API)
google-genai

# Groq (anomaly worker only)
groq
python-dotenv

# Video broadcaster
yt-dlp
