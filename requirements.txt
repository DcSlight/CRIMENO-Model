# Core
pyzmq
opencv-python
numpy
Pillow
websockets

# YOLO tracker
ultralytics

# VLM (uses torch only for the tracker; vision inference is local via Ollama below)
torch

# VLM vision — local inference via Ollama (requires the Ollama server running, see vlm/README.md)
ollama

# Groq (anomaly worker only)
groq
python-dotenv

# Video broadcaster
yt-dlp
