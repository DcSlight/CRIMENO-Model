# Core
pyzmq
opencv-python
numpy
Pillow
websockets

# YOLO tracker
ultralytics

# VLM (uses torch only for the tracker; inference is via Groq below)
torch

# Groq (anomaly worker + VLM vision — shared client/key)
groq
python-dotenv

# Video broadcaster
yt-dlp
